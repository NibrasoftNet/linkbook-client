import SparklesText from '@/components/magicui/sparkles-text';

export default function Loading() {
  return (
    <section className="flex size-full min-h-svh items-center justify-center">
      <SparklesText text="Linkbook" />
    </section>
  );
}
