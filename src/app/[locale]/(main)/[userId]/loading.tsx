import SparklesText from '@/components/magicui/sparkles-text';

export default function Loading() {
  return (
    <section className="flex h-[calc(100vh-8rem)] w-full items-center justify-center">
      <SparklesText text="Linkbook" />
    </section>
  );
}
