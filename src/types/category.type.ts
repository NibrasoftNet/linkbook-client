export type CategoryTypeValue = {
  label: string;
  value: number;
};

export type CityTypeValue = {
  label: string;
  value: string;
};

export type CommunityTypeValue = {
  label: string;
  value: string;
};
